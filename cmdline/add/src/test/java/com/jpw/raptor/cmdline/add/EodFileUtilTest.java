package com.jpw.raptor.cmdline.add;

import com.jpw.raptor.model.FileQualified;
import org.junit.Test;

import java.util.List;

/**
 * Created by john on 5/15/18.
 */
public class EodFileUtilTest {

    @Test
    public void testit() {
        /*
        EodFileUtil fu = new EodFileUtil();
        List<FileQualified> recs =  fu.getFileList();

        if ( recs != null ) {
            for (FileQualified rec : recs) {
                System.out.println(rec.getType() + " - " + rec.getName());
            }
        }
        */
    }
}
